То, что препод польский — вообще не проблема. В IT стандартом де-факто является английский язык для кода и коммитов, а эта инструкция — ваш внутренний документ (для вас троих). Но коммиты и названия веток я крайне рекомендую писать **на английском** — препод это точно оценит как профессиональный подход.

Вот максимально четкий, структурированный файл без воды. 

Создай файл **`CONTRIBUTING.md`** в корне проекта и скопируй туда этот текст:

***

```markdown
# 🛠 Правила работы с репозиторием (Git Flow)

Этот документ описывает стандартный процесс работы над проектом. Соблюдение этих правил гарантирует, что мы не сломаем код друг друга и проект всегда будет в рабочем состоянии.

## ⛔ ЗОЛОТОЕ ПРАВИЛО
**НИКТО И НИКОГДА не пушит код напрямую в ветку `main`.**
Ветка `main` — это "продакшен". Туда попадает только полностью рабочий код через Pull Request (Merge Request).

---

## 📦 БЛОК 1: Правила именования (Ветки и Коммиты)

Все названия веток и коммитов пишем **на английском языке**.

### 1. Названия веток
Формат: `тип/краткое-описание-задачи`
* `feat/...` — для новой фичи (Пример: `feat/ue5-grid-setup`, `feat/agent-movement`)
* `fix/...` — для исправления бага (Пример: `fix/python-import-error`)
* `docs/...` — для документации/ридми (Пример: `docs/update-readme`)
* `refactor/...` — переписывание кода без добавления новых фич (Пример: `refactor/agent-class-structure`)

### 2. Сообщения коммитов (Conventional Commits)
Формат: `тип: описание того что сделано (с маленькой буквы)`
* `feat: add base agent class with coordinates`
* `fix: prevent agent from moving out of bounds`
* `chore: update gitignore for unreal engine`
* `docs: add contributing guidelines`

---

## 🚀 БЛОК 2: Рабочий процесс (Шаг за шагом)

### Сценарий А: Перед началом работы (Каждый день!)
Прежде чем писать новый код, нужно забрать изменения, которые сделали другие.
```bash
# 1. Переходим в главную ветку
git checkout main

# 2. Скачиваем последние обновления
git pull origin main

# 3. Создаем свою ветку для новой задачи и переходим в нее
git checkout -b feat/название-вашей-задачи
```

### Сценарий Б: Сохранение и отправка своих изменений
Вы написали код, проверили, что он работает. Как его отправить:
```bash
# 1. Проверяем, какие файлы изменились
git status

# 2. Добавляем измененные файлы (точка = добавить все неигнорируемые)
git add .

# 3. Делаем коммит (сохраняем локально)
git commit -m "feat: short description of what you did"

# 4. Отправляем вашу ветку на сервер (в Gitea)
git push origin feat/название-вашей-задачи
```
**Шаг 5 (В браузере):** Заходим в Gitea, нажимаем **"New Pull Request"** из вашей ветки в `main`. Просим тиммейта сделать апрув (Merge).

---

## 🆘 БЛОК 3: Решение частых проблем (Что делать, если...)

**Ситуация 1: Я случайно начал писать код в ветке `main`!**
*Ничего не коммитьте.* Просто создайте новую ветку. Git сам перенесет ваши изменения в нее:
```bash
git checkout -b feat/my-new-code
```

**Ситуация 2: Git пишет `CONFLICT` при слиянии веток.**
Вы с тиммейтом изменили одну и ту же строчку. 
* Решение: Откройте файл с ошибкой в IDE (VS Code/PyCharm). IDE подсветит конфликт. Нажмите кнопку `Accept Current` (оставить ваше) или `Accept Incoming` (оставить код тиммейта). Затем:
```bash
git add .
git commit -m "fix: resolve merge conflicts"
```

**Ситуация 3: Я написал фигню, всё сломалось, хочу вернуть файлы к состоянию последнего коммита.**
*Осторожно, это удалит все несохраненные изменения!*
```bash
git restore .
```

**Ситуация 4 (Для Unreal Engine): Git не дает сделать push, пишет "File is too large" (>100MB).**
Вы случайно добавили тяжелый ассет, который не прописан в `.gitignore`.
```bash
# Отменяем последний локальный коммит (файлы останутся на диске)
git reset HEAD~1
```
После этого удалите тяжелый файл из папки, добавьте его расширение в `.gitignore`, и заново сделайте `add` и `commit`.

---

## 📂 БЛОК 4: Структура проекта (Куда класть файлы)

Строго соблюдаем разделение логики и визуала:
* `/backend` — Сервер и логика на Python (Классы Environment, Agent, API). *Ответственные: Александра, Никита.*
* `/frontend_ue` — Проект Unreal Engine 5. Никакого Python-кода здесь. *Ответственный: Артем.*
* `/frontend_backup` — Резервная 2D визуализация на случай проблем с UE5. *Ответственный: Никита.*
```

***

### Что сделать прямо сейчас (Твои действия как Лида):
1. Создай этот файл.
2. Открой терминал и введи:
   ```bash
   git add CONTRIBUTING.md
   git commit -m "docs: add comprehensive git flow and project rules"
   git push origin main
   ```
3. В основном файле `README.md` (на скрине) добавь строчку:
   **`Please read [CONTRIBUTING.md](CONTRIBUTING.md) before making any changes.`**
4. Скинь ссылку на этот файл в ваш чат с Артемом и Сашей. Скажи: *"Ребят, работаем строго по этой инструкции. Если есть вопросы по командам — смотрите туда"*.